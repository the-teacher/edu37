- handle = content_tag :div, raw('&nbsp;'), :class=>:handle, :title=>t('publications.move') if current_user.has_role? :publications, :rebuild
- link= link_to(node.title, news_url(node.zip), :title=>node.title)
- content= raw(handle.to_s) + controls + link
%div{:class=>"link#{' root' if root}"}
  = content
