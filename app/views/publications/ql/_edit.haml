- content_for :quick_links do
  %li
    = link_to t('publications.show_this'), news_url(@publication.friendly_url, :subdomain=>@user.subdomain)
    = link_to_function(image_tag('basic/ext_link.gif'), popup_window(:left=>100, :top=>100), :href=>news_url(@publication, :subdomain=>@user.subdomain))
  %li
    = link_to t('storages.title'), storages_url(:subdomain=>@user.subdomain)
    = link_to(image_tag('basic/ext_link.gif'), storages_url(:subdomain=>@user.subdomain))
