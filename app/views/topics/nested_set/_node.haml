-# node - current element of nested tree

-# opts[:class_name] - string
-# opts[:id_field] - string
-# opts[:admin] - bool
-# opts[:root] - bool
-# opts[:first] - bool
-# opts[:last] - bool
-# opts[:has_childs] - bool
-# opts[:level] - integer

=# opts[:class_name]
=# opts[:id_fileld]
=# opts[:admin].to_s
=# opts[:root].to_s
=# opts[:first].to_s
=# opts[:last].to_s
=# opts[:has_childs].to_s
=# opts[:level].to_s

:css
  .topic_ul{:margin:0;}
= content_tag(:ul, content_tag(:li, link_to(node.title, topic_path(node), :title=>node.title), :class=>"link #{'root' if root}"), :class=>:topic_ul)

