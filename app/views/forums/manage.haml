- content_for :title do
  Список форумов
  = [@user.login, Project::ROOT_DOMAIN].join('.')

- content_for :css do
  =stylesheet_link_tag 'modern/nested_set/style', :media=>'screen'

=render :partial => 'forums/ql/manage'
=render :partial => 'forums/bc/manage'

.nested_set
  = raw ans_tree(@forums, :path=>'forums/nested_set', :admin=>true, :clean=>false)
