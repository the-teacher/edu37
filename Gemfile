source 'http://rubygems.org'

# delete all gems and install required
# sudo gem list --no-versions | xargs --interactive sudo gem uninstall -a -I (y)
# sudo gem install bundler --no-ri --no-rdoc
# sudo gem install rails --no-ri --no-rdoc
# cd vendor/cache/
# sudo gem install * --no-ri --no-rdoc

# using bundler
# sudo bundle install

gem 'subdomain-fu', '1.0.0.beta2' # :git => "git://github.com/nhowell/subdomain-fu.git"
gem 'rails', '3.0.7'
gem "mail"
gem 'mysql2'
gem 'i18n', '>=0.5.0'
gem 'russian'
gem 'nokogiri'
gem 'haml'
gem "RedCloth", ">=4.0", :require => 'redcloth'
gem 'will_paginate', '~> 3.0.beta'
gem 'sanitize'
gem 'state_machine'
gem 'rmagick'
gem "paperclip", "2.3.6"
gem "haml-rails"
gem "jquery-rails"
gem 'faker'
gem 'delayed_job'
gem 'thinking-sphinx', '2.0.0', :require => 'thinking_sphinx'
gem 'ts-datetime-delta'
gem 'ts-delayed-delta'
gem 'whenever', :require => false
gem 'acts-as-taggable-on'
gem 'nested_set'
gem "puffer"

# gem 'acts-as-list'
# =========================================
# group :development, :test do
#   gem 'webrat'
# end
